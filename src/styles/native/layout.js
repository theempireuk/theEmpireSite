import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    fullScreen: {
        height: '100vh',
        width: '100vw',
    },
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    },
    fill: {
        height: '100%',
        width: '100%',
    }
});